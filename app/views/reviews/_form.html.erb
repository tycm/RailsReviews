<%= form_for([@movie, @movie.comments.build]) do |f| %>
  <p>
    <%= f.label :Reviewer %><br>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :text %>
  </p>
  <p>
    <%= f.submit %>
  </p>
  <% end %>